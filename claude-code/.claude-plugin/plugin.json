{
  "name": "bindry",
  "version": "0.2.0",
  "description": "Compile a bound Bindry Stack into Claude Code skills, so instructions load only when they're relevant instead of sitting in every prompt. Supports pinned (compiled snapshot) or live (always current via MCP) per Stack.",
  "author": {
    "name": "Bindry",
    "url": "https://bindry.ai"
  },
  "homepage": "https://bindry.ai/plugins/claude-code",
  "keywords": ["bindry", "instructions", "skills", "guardrails", "mcp", "live"]
}
